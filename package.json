{
  "name": "fenix-schemabridge-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "npm exec --yes --package=wrangler@4.131.1 -- wrangler deploy --dry-run --outdir dist/cloudflare",
    "test": "node --test",
    "start": "node tools/serve.mjs",
    "demo": "node tools/demo.mjs"
  }
}
